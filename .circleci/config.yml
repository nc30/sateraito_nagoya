version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7.10
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Build binary
          command: 'npm i'
      - run:
          name: Build binary
          command: 'npm run build'

